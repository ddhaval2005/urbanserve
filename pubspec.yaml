name: service_provider_booking
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  cloud_firestore: ^4.15.8
  lottie: ^3.0.0
  firebase_core: ^2.25.4
  firebase_auth: ^4.17.4
  google_sign_in: ^6.2.1
  sign_in_with_apple: ^7.0.1
  shared_preferences: ^2.2.2

  flutter:
    sdk: flutter



  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # ✅ ASSETS CONFIGURATION (VERY IMPORTANT)
  assets:
    - assets/images/


  # (Fonts can be added later)
